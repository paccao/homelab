---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: internal-tls
spec:
  secretName: internal-tls
  dnsNames:
    - "*.cluster.local"
  issuerRef:
    name: your-issuer
    kind: ClusterIssuer
